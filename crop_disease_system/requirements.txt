# Crop Disease Detection System — dependencies
# Reuses the parent project's trained checkpoint, class_names.json and
# disease_info.json (no training required).
opencv-python>=4.8
torch>=2.0
torchvision>=0.15
timm>=0.9
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
Pillow>=9.0
